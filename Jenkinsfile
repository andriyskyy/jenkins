pipeline {
    agent any

    environment {
        TEST = "development"
    }

    stages {
        stage('Build') {
            steps {
                sh 'printenv'
            }
        }
    }
}
